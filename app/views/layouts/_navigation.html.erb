<%= link_to "Myチャット", root_path, class: "item" %>
<div class="right menu">
	<div class="ui dropdown item">
		アカウント <i class="dropdown icon"></i>
		<div class="menu">
      <% if logged_in? %>
        <%= link_to 'ログアウト', logout_path, class: "item", data: { turbo_method: :delete } %>
      <% else %>
        <%= link_to "ログイン", login_path, class: "item" %>
      <% end %>
		</div>
	</div>
</div>